import React from 'react';
import { Repositories } from '../repositories/Repositories';

import './Home.scss';

export const Home = () => {
  return (
    <section>
      <Repositories />
    </section>
  )
};
